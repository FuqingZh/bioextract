[project]
name = "bioextract"
version = "0.1.0"
description = "Stable, provenance-aware domain access to biological database snapshots."
authors = [
    {name = "FuqingZh", email = "fu.qing.zhang.work@gmail.com"},
]
dependencies = [
    "polars>=1.39.3",
    "duckdb>=1.4.0",
]
requires-python = ">=3.13"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[dependency-groups]
dev = [
    "numpy>=2.5.1",
    "pytest>=9.0.2",
    "ruff>=0.15.14",
    "pyright>=1.1.411",
]

[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
select = [
    "E4",
    "E7",
    "E9",
    "F",
    "I",
    "UP",
    "B",
    "C4",
    "SIM",
    "EXE",
    "RUF100",
]

[tool.pytest.ini_options]
minversion = "9.0"
testpaths = [
    "tests/unit",
    "tests/contract",
    "tests/integration",
]
addopts = [
    "-ra",
    "--import-mode=importlib",
]
strict = true
markers = [
    "external_snapshot: requires explicitly configured host-owned publication files",
]

[tool.pyright]
include = ["src", "tests", "scripts"]
exclude = [".venv"]
extraPaths = ["src"]
venvPath = "."
venv = ".venv"
pythonVersion = "3.13"
typeCheckingMode = "strict"

[tool.pdm]
distribution = true

[tool.pdm.scripts]
format = "ruff format ."
format-check = "ruff format --check ."
lint = "ruff check ."
lint-fix = "ruff check --fix ."
typecheck = "pyright"
test-unit = {cmd = "pytest tests/unit", env = {PYTHONPATH = "src", BIOEXTRACT_TEST_THREADS = "4"}}
test-contract = {cmd = "pytest tests/contract", env = {PYTHONPATH = "src", BIOEXTRACT_TEST_THREADS = "4"}}
test-integration = {cmd = "pytest tests/integration", env = {PYTHONPATH = "src", BIOEXTRACT_TEST_THREADS = "4"}}
test-smoke = {cmd = "pytest tests/smoke", env = {PYTHONPATH = "src", BIOEXTRACT_TEST_THREADS = "2"}}
test = {cmd = "pytest tests/unit tests/contract tests/integration", env = {PYTHONPATH = "src", BIOEXTRACT_TEST_THREADS = "4"}}
lock-check = "pdm lock --check"
check = {composite = ["lock-check", "format-check", "lint", "typecheck", "test"]}
precommit = {composite = ["format", "lint-fix", "typecheck", "test"]}
