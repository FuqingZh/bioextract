# bioextract Tree Review

Version: v1.1
Date: 2026-08-26
Status: recorded tree review; not current architecture

Reviewed commit: `64a88027159b0267bd2db72aa8c43a9f9838f226`
(`Merge pull request #57 from FuqingZh/codex-reactome-unmatched-schema`)

Remediation plan:
[tree-review remediation implementation plan](../implementation-plans/20260826-v1.0-tree-review-remediation-implementation-plan.md)

Revision note: v1.1 incorporates contract review against the current Reactome,
GO, ChEBI, Rhea, and repository-wide domain-access authorities. It withdraws
the former Reactome NCBI normalization suggestion, separates observed defects
from contract decisions, and narrows the lazy-execution and metadata findings.

## Decision Boundary

This document records a read-only assessment of product narrative, domain
architecture, implementation correctness, testing, and package identity. It
does not supersede the
[Domain Access Architecture](20260729-v1.0-domain-access-architecture.md)
or the [Materialized Dataset Contract](tidy-dataset-contract.md). It does not
change public APIs, publication schemas, or formal CephFS delivery.

If this review and an approved contract disagree about runtime behavior, the
approved contract remains authoritative until a later versioned successor
updates it. If this review and implementation disagree, treat the disagreement
as a defect against the approved contract or against this recorded finding.

This review does not authorize package publication, formal snapshot
replacement, or deletion of old artifacts.

## Verdict

`bioextract` is an independent domain-access library: callers supply official
local snapshots; the library hides resource-owned identifiers, joins, and
hierarchies; storage is a strategy (native files or one DuckDB per logical
product). The independence boundary holds in executable code.

The highest-priority defects are honesty defects, not missing resources. The
root README still advertises metadata v1 and deleted `extract_*` terminals.
The development tree and editable distribution report `0.1.0`, while the
repository has a `0.7.0` release tag, release records name `0.6.0`/`0.7.0`, and
the retained `dist/` artifacts and freeze file name `0.5.0`. Provenance can
therefore record a package identity that is neither the last release nor an
explicitly declared next development version.

Approximate inspected size: about 31,404 Python lines under `src/bioextract`
and 13,734 under `tests/`.

## Scope And Method

The review read `AGENTS.md`, both READMEs, `docs/README.md`, the domain
architecture, the tidy-dataset contract, publication and runtime modules, and
representative resource adapters (GO, KEGG, Reactome, UniProt, ChEBI, STRING,
eggNOG, OmniPath). Contract tests were used as the lock on the current public
surface.

It is not a full real-snapshot smoke, not a formal-release closeout, and not
an enrichment-analysis review. Incomplete package-release and CephFS
replacement records remain as previously written.

## Product And Architecture

### Strengths

- No downloader, no `biofetch` import, no enrichment p-values in `src/`.
  Runtime dependencies are Polars, DuckDB, and PyArrow.
- Public surface is eleven `*Database` handles plus `inspect_publication`,
  loaded lazily from `src/bioextract/__init__.py`.
- eggNOG, STRING, and OmniPath remain native-file strategies. GO, KEGG,
  Reactome, UniProt, ChEBI, Rhea, WikiPathways, and InterPro publish DuckDB.
- Writers stage, `CHECKPOINT`, reopen/validate, then `os.replace`. Biological
  relations live in `main`; provenance is exactly five `_bioextract` tables.
- Metadata v2 in code rejects `bioextract.sources` and requires paired
  `release_version` / `release_version_source`.
- KEGG organism mapping is a true dual path: shared relation nouns over Polars
  source scans and a DuckDB-native publisher.
- Tests lock the break from the old surface: no `extract_*`, no `read_mapping`,
  no directory/`write_parquet` writers.

### Findings

| ID | Severity | Finding |
| --- | --- | --- |
| P-1 | defect | Root `README.md:33` and `README.zh-CN.md:33` state that metadata v1 is the only supported publication contract. Code uses `METADATA_SCHEMA_VERSION = "2"` (`src/bioextract/_publication.py:15`) and rejects v1 keys. |
| P-2 | defect | README examples call `extract_compounds()`, `extract_ancestors()`, `extract_mapping()`, and UniProt `read_mapping()` (`README.md:97-100`, `:186`, `:285`, `:329`). Contract tests forbid those names. Current terminals are noun methods such as `compounds()`, `ancestors()`, `mappings()`, `scan_mapping()`, and `unmatched_ids()`. |
| P-3 | defect | Package identity is split: `pyproject.toml` `0.1.0`, `dist/` `0.5.0`, docs and Reactome plans `0.6.0`/`0.7.0`. `_package_version()` writes `importlib.metadata.version("bioextract")` or `"unknown"` into provenance (`src/bioextract/_publication.py:761-765`). |
| P-4 | defect | The current GO/KEGG architecture still says metadata-v1 provenance (`docs/architecture/go-kegg-tidy.md:120-121`) although current writers are v2-only. The completed publication-inspection plan accurately records its historical metadata-v1 contract and must not be rewritten as if it had originally implemented v2. |
| P-5 | suggestion | GO tidy frames use `edge` / `synonym` / `xref`; DuckDB tables remap to `term_relation` / `term_synonym`. `build_tidy().frames` and `write_duckdb().tables` do not share names. |
| P-6 | observation | `GODatabase.select_terms()` is an eager selector, not one of the public relation-noun terminals covered by the repository-wide `LazyFrame` rule. Source-backed ancestor selection uses the documented ontology-closure materialization exception and returns a replayable lazy terminal. No contract defect is established without workload evidence showing that the explicit exception is no longer bounded enough. |
| P-7 | observation | STRING/OmniPath have no `write_duckdb`, but errors say “publication”. Native strategy and materialized strategy are verbally collapsed. |
| P-8 | observation | Formal-release and Reactome capability plans remain separately authorized for package/CephFS closeout. This review does not convert those records into passes. |

The independence boundary holds in executable code. Documentation leaks
(“enrichment inputs” in a Reactome docstring; eggNOG architecture
“enrichment-input extraction”) are wording, not analysis code.

## Scientific And Implementation Correctness

### C-1A — Severity: bug

- File: `src/bioextract/go/go.py:605-613`
- Description: GO treats any filename containing `.obo` as ontology input, so
  names such as `go.obo.bak` are candidates even though they are not declared
  OBO transports.
- Suggestion: Match an explicit allow-list such as `.obo`, `.obo.gz`, and the
  already supported archive transports.

### C-1B — Severity: contract decision

- File: `src/bioextract/go/go.py`, `src/bioextract/chebi/chebi.py`,
  `src/bioextract/rhea/rhea.py`, `src/bioextract/kegg/metabolic/core.py`
- Description: Several directory constructors recursively discover official
  release roles without an explicit depth or candidate-count contract. This is
  an observable scale and traversal risk, but the agent-only `/cephfs_data`
  search rule does not by itself define runtime API behavior.
- Suggestion: Specify each resource's accepted release layouts and discovery
  root before adding limits. Prefer role-targeted traversal and suffix
  allow-lists; any depth, count, or byte threshold needs a named rationale and
  an override path so valid official layouts are not rejected accidentally.

### C-2 — Severity: bug

- File: `src/bioextract/go/ontology/derive.py`
- Description: Hierarchical parents missing from `term` are counted in
  indegree while distance maps are initialized only from term IDs. A dangling
  `is_a` / `part_of` target raises a raw `KeyError` during source-backed graph
  derivation.
- Suggestion: Reject edges whose endpoints are absent from `term` before graph
  derivation. Use the owning source-input error contract for source builds;
  reserve `IntegrityError` for an invalid or replaced publication unless the GO
  architecture explicitly establishes a broader meaning.

### C-3 — Severity: bug

- File: `src/bioextract/uniprot/_query.py:836-841`,
  `src/bioextract/uniprot/uniprot.py`
- Description: Shared `normalize_input_id` unwraps `sp|P04637|…`, but its name
  and docstring imply a generic identifier normalizer even though the behavior
  is UniProt-specific. Reactome applies it only for `namespace="uniprot"`;
  Rhea independently implements equivalent pipe parsing; UniProt knowledgebase
  and idmapping selections only strip, so pipe-form accessions become
  unmatched.
- Suggestion: Define one private UniProt-specific normalizer and apply it only
  at declared `uniprot` boundaries. Keep exact-text namespaces exact after
  trimming, and keep ChEBI, Rhea, and GO canonicalization in the namespace or
  resource that owns those grammars.

### C-4 — Severity: bug

- File: `src/bioextract/kegg/kegg.py:230-252`
- Description: `from_duckdb` stores a path only. GO/Reactome/ChEBI pin
  `(dev, ino, size, mtime, ctime)` and refuse a replaced file. Replacing a
  KEGG DuckDB while a handle is live silently changes query results.
- Suggestion: Apply the same publication-identity assert as GO `connect()`.

### C-5 — Withdrawn

- File: `src/bioextract/reactome/reactome.py`
- Original suggestion: Canonicalize or reject Reactome NCBI identifiers.
- Contract review: Withdrawn. `docs/architecture/reactome-db.md` explicitly
  defines `ncbi` as any non-empty trimmed official text and deliberately does
  not rewrite prefixes because the official snapshot contains Gene ID,
  GenBank, and RefSeq-style values. ChEBI and GtoP have different namespace
  grammars; their canonicalization does not imply an NCBI rule.

### C-6 — Severity: suggestion

- File: `src/bioextract/_tidy.py:150`
- Description: `resource_name or self.build_id_prefix` contradicts the rule
  that `build_id_prefix` never supplies resource identity. Public writers set
  `resource_name`; the fallback is a provenance footgun.
- Suggestion: Require `resource_name`.

### C-7 — Severity: suggestion

- File: `src/bioextract/_publication.py:761-765`,
  `src/bioextract/rhea/_duckdb.py:729-732`
- Description: Missing install metadata becomes provenance value `"unknown"`
  in both the shared publication writer and Rhea's specialized writer.
- Suggestion: Fail publication if the distribution is not installed, or record
  an explicit unresolved key that cannot be mistaken for a release.

## Testing

Contract, unit, and hermetic integration layers match the test standard.
`tests/contract/api/` locks `__all__`, lazy import, and the post-`extract_*`
surface. Atomic replace and metadata v2 rejection are covered.

Gaps aligned with the defects above: namespace-scoped UniProt pipe IDs, GO
dangling parents and false-positive OBO candidates, and KEGG publication
replacement. Directory-discovery limits require an owning contract before an
overflow fixture can encode expected behavior. Smoke against host-owned
snapshots remains opt-in and is out of this review's execution.

## What Remains Solid

- No `shell=True`. No hardcoded `/home/fqzhang` or `/cephfs` under `src/`.
- User identifiers bind through placeholders or temporary tables. Interpolated
  SQL names pass `_SQL_IDENTIFIER` or come from internal schema maps.
- Extra metadata cannot overwrite canonical publication keys.
- Unmatched-ID accounting exists on GO, ChEBI, and KEGG mapping with reason
  codes.
- Filename is not schema identity.

## Out Of Scope

This review does not authorize a new resource adapter, enrichment statistics,
generic query facade, CephFS publication, or converting the Reactome v0.5 /
formal-release remaining gates into passes.

## Remediation

Do not treat this document as an implementation queue. Execute findings only
through the
[remediation plan](../implementation-plans/20260826-v1.0-tree-review-remediation-implementation-plan.md).
Public API, publication schema, or README contract changes must update the
owning contract and tests together.
